# Test configuration for models package
# Tests can run in parallel - each has isolated in-memory storage
concurrency: 4

# Test timeout
timeout: 30s

# Platform configuration
platforms:
  - vm

# Reporter configuration
reporter: compact